// Rafeeq FE → BE connection (PRODUCTION). Set these to the hosted API when you deploy.
export const environment = {
  production: true,
  API_URL: 'https://<your-prod-host>/api',
  API_BASE_URL: 'https://<your-prod-host>/',
};
