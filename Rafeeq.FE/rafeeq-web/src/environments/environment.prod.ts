// Rafeeq FE → BE connection (PRODUCTION).
// After the API is deployed on Render, replace the host below with your Render URL
// (e.g. https://rafeeq-api.onrender.com) and re-run the Pages workflow.
export const environment = {
  production: true,
  API_URL: 'https://rafeeq-api.onrender.com/api',
  API_BASE_URL: 'https://rafeeq-api.onrender.com/',
};
